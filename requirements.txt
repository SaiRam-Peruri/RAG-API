# Minimal requirements for rag_api_simple.py deployment

# Web framework
fastapi==0.115.0

# ASGI server (to run the API)
uvicorn[standard]==0.32.0

# Vector database (updated for Python 3.12 compatibility)
chromadb==0.5.20

# Data validation
pydantic==2.10.3
